Execute the following steps to read the project documentation:
1 Read `README.md` in the project root.  
2 Read documentation:  
 2.1 `docs/README.md`  
 2.2 List files under `docs/` with `ls docs/`  
 2.3 Read all files under `docs/`  
3 Inspect repository structure with `git ls-files` (read‑only).