import React from 'react';

interface QueryState {
  name: string;
  query: string;
  startTime?: number;
  endTime?: number;
}

const QueryTimeline = () => {
  return <div>QueryTimeline</div>;
};

export default QueryTimeline;
